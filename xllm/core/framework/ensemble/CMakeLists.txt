include(cc_library)
include(cc_test)

cc_library(
  NAME
    graph_config
  HDRS
    graph_config.h
  SRCS
    graph_config.cpp
  DEPS
    glog::glog
    nlohmann_json::nlohmann_json
)

cc_library(
  NAME
    graph
  HDRS
    graph.h
  SRCS
    graph.cpp
  DEPS
    graph_config
    glog::glog
)

cc_library(
  NAME
    engine_config
  HDRS
    engine_config.h
  SRCS
    engine_config.cpp
  DEPS
    graph_config
    :common
    :util
    gflags::gflags
    glog::glog
)

cc_test(
  NAME
    graph_config_test
  SRCS
    graph_config_test.cpp
  DEPS
    graph_config
    GTest::gtest_main
)
